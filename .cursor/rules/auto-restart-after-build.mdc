---
description: 编译/同步发布后自动结束并重开 USB急救工具
alwaysApply: true
---

# 编译后自动重开软件

用户要求：每次完成编译、发布同步（`发布\`）后，**自动结束正在运行的本工具并重新启动**，不要只提示用户手动重开。

## 操作步骤

1. 结束进程：匹配 `发布\USB急救工具.exe` 的路径，或主窗口标题含「一体化急救 / USB 修复 / USB急救 / 解除占用」。不要误杀其它无关 USB 软件（如厂商工具）。
2. 等待约 1 秒，把最新 `publish\USB急救工具.exe` 与 `wwwroot`、`lib`、畅通匣脚本同步到 `发布\`。
3. `Start-Process` 启动 `发布\USB急救工具.exe`（WorkingDirectory 为 `发布\`）。
4. 若文件仍被锁，先强制结束再复制；仍失败再告知用户。

仅改前端 `wwwroot` 时同样重开，确保 WebView 加载到新资源。
